# Notifications Epic Scope Closeout

This document tracks the acceptance criteria for the "configurable PR changes requested" task that were explicitly scoped out.

### Out of scope (No existing channel infrastructure)
These channels have no existing integration or sending infrastructure in this bot. Implementing them would require a separate epic.
- **SMS**: won't do — no existing channel infrastructure, out of scope.
- **Digest email**: won't do — no existing channel infrastructure, out of scope.
- **Push notification**: won't do — no existing channel infrastructure, out of scope.
- **RSS entry**: won't do — no existing channel infrastructure, out of scope.
- **Webhook**: won't do — no existing channel infrastructure, out of scope.
- **Pinned message**: won't do — no existing channel infrastructure, out of scope.
- **Admin channel**: won't do — no existing channel infrastructure, out of scope.

### Out of scope (Combination not selected for this event)
These formats/channels exist conceptually but were not chosen for the "PR changes requested" event based on current usage patterns.
- **Voice message**: won't do — combination not selected for this event.
- **JSON payload**: won't do — combination not selected for this event.
- **Ticker line**: won't do — combination not selected for this event.
- **Rich card+image**: won't do — combination not selected for this event.
- **Threaded reply**: won't do — combination not selected for this event.
